# Architecture

Two Workers. FE: Angular 22 SSR (`platform: neutral`). BE: Hono + Better Auth OTP. Tooling: Wrangler 4+.

Bindings: D1, KV, R2, Images, EMAIL, Queue, Durable Object SchedulerLock, Cron, Vectorize, Workers AI, Analytics Engine.

See README for the request path.
