{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "sundraft-api",
  "main": "src/index.ts",
  "compatibility_date": "2026-09-22",
  "compatibility_flags": ["nodejs_compat"],
  "observability": { "enabled": true },
  "vars": {
    "APP_NAME": "Sundraft",
    "WEB_ORIGIN": "https://sundraft.xyz"
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "sundraft",
      "database_id": "REPLACE_D1_ID",
      "migrations_dir": "migrations"
    }
  ],
  "kv_namespaces": [
    { "binding": "KV", "id": "REPLACE_KV_ID" }
  ],
  "r2_buckets": [
    { "binding": "MEDIA", "bucket_name": "sundraft-media" }
  ],
  "send_email": [{ "name": "EMAIL", "remote": true }],
  "queues": {
    "producers": [{ "binding": "PUBLISH", "queue": "sundraft-publish" }],
    "consumers": [
      {
        "queue": "sundraft-publish",
        "max_retries": 8,
        "dead_letter_queue": "sundraft-publish-dlq"
      }
    ]
  },
  "durable_objects": {
    "bindings": [{ "name": "SCHEDULER_LOCK", "class_name": "SchedulerLock" }]
  },
  "migrations": [{ "tag": "v1", "new_sqlite_classes": ["SchedulerLock"] }],
  "triggers": { "crons": ["* * * * *"] },
  "analytics_engine_datasets": [{ "binding": "METRICS", "dataset": "sundraft_metrics" }],
  "vectorize": [{ "binding": "SEARCH", "index_name": "sundraft-posts" }],
  "ai": { "binding": "AI" },
  "images": { "binding": "IMAGES" }
}
